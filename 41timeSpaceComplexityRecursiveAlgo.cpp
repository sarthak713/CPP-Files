#include<iostream>
using namespace std;

int main(){

/* 
Code Studio Time & Space Complexity
Time & Space Complexity of Recursive ALgorithms

1. Factorial
    TC = O(n)
    SC = O(n)
2. Binary Search
    TC = O(log n)
    SC = O(log n)
3. Merge Sort
    TC = O(n log n)
    SC = O(n)
4. Fibonacci
    TC = O(2^n)
    SC = O(n)

*/
    
    
    return 0;
}